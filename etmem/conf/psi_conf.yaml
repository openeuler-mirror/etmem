[project]
name=test
scan_type=psi
interval=6

[engine]
name=psi
project=test

[task]
project=test
engine=psi
type=name
value=cgroup_v1
name=psi_task
cg_dir=isulad/*
pressure=0.1
limit_min_bytes=209715200
